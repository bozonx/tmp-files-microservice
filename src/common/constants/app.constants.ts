// Timeout for app.close() during graceful shutdown (60 seconds)
export const APP_CLOSE_TIMEOUT_MS = 25000

// Maximum number of files to process in a single cleanup batch
export const CLEANUP_BATCH_SIZE = 1000
