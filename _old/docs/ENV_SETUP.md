# ⚙️ Настройка переменных окружения

Полное руководство по настройке переменных окружения для micro-file-cache.

## 📁 Файлы конфигурации

В проекте доступны следующие файлы конфигурации:

| Файл          | Описание                                        | Использование |
| ------------- | ----------------------------------------------- | ------------- |
| `env.example` | Пример конфигурации с описанием всех переменных | Справочник    |
| `env.dev`     | Готовая конфигурация для разработки             | Разработка    |
| `env.docker`  | Конфигурация для Docker                         | Docker        |
| `env.test`    | Конфигурация для тестов                         | Тестирование  |

## 🚀 Быстрая настройка для разработки

### 1. Создание файла .env

```bash
# Скопируйте готовую конфигурацию для разработки
cp env.dev .env
```

### 2. Запуск приложения

```bash
# Запустите в режиме разработки
pnpm run start:dev
```

✅ **Готово!** Сервис запущен с настройками по умолчанию.

## 🔐 Настройка аутентификации

### Включение аутентификации (по умолчанию)

В файле `env.dev` уже настроена аутентификация:

```env
AUTH_ENABLED=true
AUTH_TOKEN=dev-secret-key-for-micro-file-cache-12345678901234567890
```

### Отключение аутентификации

Для отключения аутентификации в файле `.env`:

```env
AUTH_ENABLED=false
```

### Тестирование аутентификации

#### Health endpoint (без аутентификации)

```bash
curl -X GET http://localhost:3000/api/v1/health
```

#### Защищенные endpoints (требуют токен)

```bash
# Без токена (должен вернуть 401)
curl -X GET http://localhost:3000/api/v1/files

# С валидным токеном
curl -X GET http://localhost:3000/api/v1/files \
  -H "Authorization: Bearer dev-secret-key-for-micro-file-cache-12345678901234567890"
```

## 🌐 Настройка базового пути API

Для размещения нескольких микросервисов на одном хосте:

```env
API_BASE_PATH=file-cache
API_VERSION=v1
```

В этом случае API будет доступен по пути `/file-cache/v1/` вместо `/api/v1/`.

## 📋 Полный список переменных окружения

### 🔧 Основные настройки

| Переменная      | Описание               | По умолчанию  | Обязательная |
| --------------- | ---------------------- | ------------- | ------------ |
| `NODE_ENV`      | Режим работы           | `development` | ❌           |
| `LISTEN_HOST`   | Хост для прослушивания | `localhost`   | ❌           |
| `LISTEN_PORT`   | Порт для прослушивания | `3000`        | ❌           |
| `API_BASE_PATH` | Базовый путь для API   | `api`         | ❌           |
| `API_VERSION`   | Версия API             | `v1`          | ❌           |

### 🔐 Аутентификация

| Переменная     | Описание                          | По умолчанию | Обязательная |
| -------------- | --------------------------------- | ------------ | ------------ |
| `AUTH_ENABLED` | Включить/выключить аутентификацию | `true`       | ❌           |
| `AUTH_TOKEN`   | Секретный ключ для аутентификации | -            | ✅\*         |

\*Обязательна если `AUTH_ENABLED=true`, в production режиме минимум 32 символа

### 💾 Хранилище

| Переменная             | Описание                                 | По умолчанию    | Обязательная |
| ---------------------- | ---------------------------------------- | --------------- | ------------ |
| `STORAGE_DIR`          | Путь к директории хранилища              | -               | ✅           |
| `MAX_FILE_SIZE_MB`     | Максимальный размер файла в мегабайтах   | `100`           | ❌           |
| `ALLOWED_MIME_TYPES`   | Разрешенные MIME типы файлов             | `[]` (все типы) | ❌           |
| `ENABLE_DEDUPLICATION` | Включить дедупликацию файлов             | `true`          | ❌           |
| `MAX_STORAGE_SIZE_MB`  | Максимальный общий размер хранилища в МБ | `1000`          | ❌           |

### ⏰ TTL настройки

| Переменная    | Описание                                                                                  | По умолчанию | Обязательная |
| ------------- | ----------------------------------------------------------------------------------------- | ------------ | ------------ |
| `MAX_TTL_MIN` | Максимальное время жизни файла в минутах (7 дней, обязательно указывать ttl при загрузке) | `10080`      | ❌           |

### 🧹 Очистка

| Переменная     | Описание                   | По умолчанию     | Обязательная |
| -------------- | -------------------------- | ---------------- | ------------ |
| `CLEANUP_CRON` | Cron выражение для очистки | `0 */10 * * * *` | ❌           |

### 📝 Логирование

| Переменная  | Описание            | По умолчанию | Обязательная |
| ----------- | ------------------- | ------------ | ------------ |
| `LOG_LEVEL` | Уровень логирования | `info`       | ❌           |

**Доступные уровни:** `error`, `warn`, `info`, `debug`, `verbose`

### 🌐 CORS настройки

| Переменная    | Описание       | По умолчанию | Обязательная |
| ------------- | -------------- | ------------ | ------------ |
| `CORS_ORIGIN` | CORS настройки | `true`       | ❌           |

### 🕐 Настройки timezone

| Переменная | Описание                         | По умолчанию | Обязательная |
| ---------- | -------------------------------- | ------------ | ------------ |
| `TZ`       | Часовой пояс для работы с датами | `UTC`        | ❌           |

## 🔒 Безопасность

⚠️ **Важно**: В продакшене обязательно измените `AUTH_TOKEN` на более сложный и безопасный ключ!

### Рекомендации по безопасности

1. **Секретный ключ**:
   - Минимум 32 символа в production
   - Используйте случайные символы
   - Не храните в коде или публичных репозиториях

2. **Переменные окружения**:
   - Используйте `.env` файлы для локальной разработки
   - В production используйте системные переменные окружения
   - Не коммитьте `.env` файлы в git

## 📝 Примеры конфигурации

### Минимальная конфигурация

```env
# Основные настройки
NODE_ENV=development
LISTEN_PORT=3000

# Аутентификация
AUTH_ENABLED=true
AUTH_TOKEN=your-secret-key-here

# Хранилище
STORAGE_DIR=./storage
```

### Полная конфигурация для разработки

```env
# Основные настройки
NODE_ENV=development
LISTEN_HOST=localhost
LISTEN_PORT=3000
API_BASE_PATH=api
API_VERSION=v1

# Аутентификация
AUTH_ENABLED=true
AUTH_TOKEN=dev-secret-key-for-micro-file-cache-12345678901234567890

# Хранилище
STORAGE_DIR=./storage
MAX_FILE_SIZE_MB=100
ALLOWED_MIME_TYPES=[]
ENABLE_DEDUPLICATION=true
MAX_FILES_COUNT=10000
MAX_STORAGE_SIZE_MB=1000

# TTL настройки
MAX_TTL_MIN=10080

# Очистка
CLEANUP_CRON=0 */10 * * * *

# Логирование
LOG_LEVEL=debug

# CORS настройки
CORS_ORIGIN=true

# Настройки timezone
TZ=UTC
```

### Конфигурация для production

```env
# Основные настройки
NODE_ENV=production
LISTEN_HOST=0.0.0.0
LISTEN_PORT=3000

# Аутентификация
AUTH_ENABLED=true
AUTH_TOKEN=your-very-secure-production-key-minimum-32-characters-long

# Хранилище
STORAGE_DIR=/app/storage
MAX_FILE_SIZE_MB=100
ALLOWED_MIME_TYPES=["image/jpeg","image/png","application/pdf","text/plain"]
ENABLE_DEDUPLICATION=true
MAX_FILES_COUNT=10000
MAX_STORAGE_SIZE_MB=1000

# TTL настройки
MAX_TTL_MIN=1440

# Очистка
CLEANUP_CRON=0 */5 * * * *

# Логирование
LOG_LEVEL=info

# CORS настройки
CORS_ORIGIN=true

# Настройки timezone
TZ=UTC
```

## 🆘 Решение проблем

### Сервис не запускается

1. **Проверьте обязательные переменные**:

   ```bash
   # Убедитесь, что STORAGE_DIR установлен
   echo $STORAGE_DIR

   # Если AUTH_ENABLED=true, проверьте AUTH_TOKEN
   echo $AUTH_TOKEN
   ```

2. **Проверьте права доступа**:
   ```bash
   # Убедитесь, что папка storage существует и доступна для записи
   mkdir -p ./storage
   chmod 755 ./storage
   ```

### Ошибки аутентификации

1. **Проверьте настройки**:

   ```bash
   # Убедитесь, что AUTH_ENABLED=true
   grep AUTH_ENABLED .env

   # Проверьте, что AUTH_TOKEN установлен
   grep AUTH_TOKEN .env
   ```

2. **Проверьте токен в запросах**:
   ```bash
   # Используйте правильный токен из .env файла
   curl -H "Authorization: Bearer $(grep AUTH_TOKEN .env | cut -d'=' -f2)" \
     http://localhost:3000/api/v1/health
   ```

### Проблемы с хранилищем

1. **Проверьте путь к хранилищу**:

   ```bash
   # Убедитесь, что путь существует
   ls -la $(grep STORAGE_DIR .env | cut -d'=' -f2)
   ```

2. **Проверьте права доступа**:
   ```bash
   # Убедитесь, что приложение может писать в папку
   touch $(grep STORAGE_DIR .env | cut -d'=' -f2)/test.txt
   rm $(grep STORAGE_DIR .env | cut -d'=' -f2)/test.txt
   ```

### Проблемы с timezone

1. **Проверьте настройку timezone**:

   ```bash
   # Убедитесь, что TZ установлен
   echo $TZ

   # Проверьте доступные timezone в системе
   ls /usr/share/zoneinfo/
   ```

2. **Проблемы с датами в Docker**:
   - Убедитесь, что в Dockerfile установлен пакет `tzdata`
   - Проверьте, что переменная `TZ` установлена в docker-compose.yml
   - Все даты в приложении обрабатываются в UTC для консистентности

## 📚 Дополнительные ресурсы

- **[Быстрый старт](QUICK_START.md)** - пошаговое руководство по установке
- **[Примеры использования](QUICK_EXAMPLES.md)** - примеры основных операций
- **[Docker документация](DOCKER_USAGE.md)** - настройка для Docker

---

**Готово!** Теперь вы знаете, как настроить переменные окружения для micro-file-cache. 🎉
